import { describe, it } from "vitest";

describe("externally verified four-pillar reference corpus", () => {
  it.skip("runs after the 20 candidate cases receive authoritative expected pillars", () => {
    // DECISION REQUIRED: tests/fixtures/saju-reference.json intentionally has
    // no fabricated expected values. Enable data-driven assertions only after
    // each case records its source and applicable calculation conventions.
  });
});

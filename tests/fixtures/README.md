# Saju reference fixtures

`saju-reference.json` contains 20 **candidate inputs**, but every `expected` value is intentionally `null`. Populate an expected pillar only after it has been verified against an external authoritative source, including the precise timezone, birth time, solar-term convention, and day-boundary convention. These cases are not active tests. Do not derive expected values from this engine.
